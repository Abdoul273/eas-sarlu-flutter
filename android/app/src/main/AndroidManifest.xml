<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Déverrouillage par empreinte ou visage. Sans cette permission, le
         capteur reste muet et l'application retombe silencieusement sur la
         saisie du code, sans que rien n'explique pourquoi. -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC"/>
    <!-- Les téléphones antérieurs à Android 9 n'exposent que l'API empreinte. -->
    <uses-permission android:name="android.permission.USE_FINGERPRINT"/>
    <!-- L'application sert d'abord hors ligne : la connexion n'est pas requise
         pour l'ouvrir, seulement pour synchroniser. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <!-- Depuis Android 13, une application qui ne déclare PAS cette permission
         n'affiche aucune notification : pas d'erreur, pas de journal, rien.
         Le système de notifications d'activité était donc entièrement muet sur
         tous les téléphones récents, alors que le badge, lui, se remplissait. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <application
        android:label="E.A.S Sarlu"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Depuis Android 11, une application ne voit les autres que si elle
             les déclare. Sans cette entrée, le moteur de synthèse vocale du
             téléphone est invisible et l'assistant reste muet sans dire
             pourquoi. -->
        <intent>
            <action android:name="android.intent.action.TTS_SERVICE"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="tel"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="whatsapp"/>
        </intent>
        <package android:name="com.whatsapp"/>
        <package android:name="com.whatsapp.w4b"/>
    </queries>
</manifest>
